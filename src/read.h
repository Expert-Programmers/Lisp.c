//
// Created by me on 17-4-15.
//

#ifndef SIMPLERPEL_READ_H
#define SIMPLERPEL_READ_H

#include "base.h"

list ReadExp(char c);

list Print(list a);

char SkipSpace(char c);

list ReadInt(char c);

list ReadAtom(char c);

list ReadCons(char c);

list ReadExp(char c);

list Read(void);

#endif //SIMPLERPEL_READ_H
